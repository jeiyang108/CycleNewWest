<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/colorPrimaryDark"
    android:weightSum="1">


    <ImageView
        android:id="@+id/loading_bike"
        android:layout_width="wrap_content"
        android:layout_height="460dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentStart="true"
        android:layout_marginTop="0dp"
        android:layout_weight="0.42"
        android:src="@raw/loading" />

    <Button
        android:id="@+id/moveToMain"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignBottom="@+id/loading_bike"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="51dp"
        android:background="@color/colorDark"
        android:paddingLeft="15dp"
        android:paddingRight="15dp"
        android:text="Let's start!"
        android:textColor="@color/colorBase"
        android:textColorLink="@color/colorDark"
        android:layout_gravity="center"/>

    <!--
    <TextView
        android:id="@+id/apptitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="65dp"
        android:fontFamily="sans-serif-smallcaps"
        android:text="CYCLE NEW WEST"
        android:textColor="@color/colorBase"
        android:textColorLink="@color/colorBase"
        android:textSize="34sp"
        android:textStyle="bold"
        android:layout_alignTop="@+id/loading_bike"
        android:layout_centerHorizontal="true" />
    -->
</LinearLayout>